# Web framework
flask>=3.0
Werkzeug==3.1.3
gunicorn>=21.0

# Database
pymongo==4.14.1
flask-pymongo>=2.3

# Auth
authlib>=1.3

# Sessions & rate-limiting
flask-session>=0.8
flask-limiter>=3.5

# Payments
stripe>=9.0

# Scheduler (background GEX collection process)
apscheduler>=3.10

# Twitter distribution job
tweepy>=4.14

# Data sources
schwab_py==1.5.0
yfinance==0.2.61
kiteconnect==5.2.1

# Frontend/data utilities
flask_wtf==1.2.2
openpyxl==3.1.5
plotly==5.24.1
pytz==2025.2
Requests==2.32.5

# Finance utilities
pandas_market_calendars==5.4.0
scipy>=1.10.0

# Env / config
# fastmcp (below) requires a newer python-dotenv than the previous 1.0.0 pin.
python-dotenv>=1.2.3

# MCP server (Module 11 — external agent access via mcp_server/server.py)
fastmcp>=3.4

# Transactional email (Module 01 — verification & password reset)
sendgrid>=6.11

# Chart rendering for Module 10 social posts
matplotlib>=3.8
Pillow>=10.0

# --- Optional / future modules (uncomment as needed) ---
# langchain_ollama==0.3.7
# pandasai==2.3.2
# pandas==2.3.2
# pandas_ta==0.3.14b0
# numpy==2.3.2
# seaborn==0.13.2
# talib==0.6.8
# pandas_xlsx_tables==1.0.0
